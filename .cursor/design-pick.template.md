# Design pick (template)

Copy or @-mention this file when sending picks from Agent Picker.

When a pick is sent, the latest selection is written to `.cursor/design-pick.md` (gitignored).

Toggle edit mode: **⌘⇧E** on any localhost Vite page, or add `?agentPick=1`.
